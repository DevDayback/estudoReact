
import './Banner.css'
export const Banner = () => {
    // JSX
    return(
        <header className="Banner"> 
            <img src="/imagens/headerDayback.jpeg" alt = "Banner principal da página do learningrr"/>
        </header>

    )
}

// substituindo isso pela const export default Banner